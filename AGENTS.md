## PR Rules

- PR 제목과 본문에 AI 도구 관련 문구를 포함하지 않는다.
- 커밋 메시지에 Co-Authored-By AI 서명을 추가하지 않는다.
- PR 본문에 AI 도구의 세션 링크를 포함하지 않는다.
- PR은 항상 base를 `dev`로 생성한다. `main`으로 직접 PR 생성 금지.
- `gh pr create` 실행 시 반드시 `--base dev` 옵션을 사용한다.
