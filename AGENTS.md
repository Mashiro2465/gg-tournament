## PR Rules

- PR 제목과 본문에 AI 도구 관련 문구를 포함하지 않는다.
- 커밋 메시지에 Co-Authored-By AI 서명을 추가하지 않는다.
- PR 본문에 AI 도구의 세션 링크를 포함하지 않는다.
- PR은 항상 base를 `dev`로 생성한다. `main`으로 직접 PR 생성 금지.
- `gh pr create` 실행 시 반드시 `--base dev` 옵션을 사용한다.
- Codex의 기본 작업 범위는 커밋, 푸시, PR 생성까지이며 PR 생성 후에는 URL과 검증 결과를 보고하고 멈춘다.
- 사용자가 현재 요청에서 특정 PR의 머지를 명시적으로 지시하지 않는 한 `gh pr merge`, 자동 머지 활성화, 웹 UI 머지를 실행하지 않는다.
- 기능 구현이나 "둘 다 해줘" 같은 포괄적 요청은 PR 머지 권한으로 해석하지 않는다.
- `feat/* → dev`와 `dev → main` 머지는 사용자가 GitHub에서 직접 검토하고 실행하는 것을 원칙으로 한다.
